function usuario(name,ape,fecha,tele,correo){
    let user = {
        nombre: name,
        apellido: ape,
        fechanac: fecha,
        telefono: tele,
        correo: correo
    }
    console.log(user);
}